export enum OverlayRoutes {
  Menu = 'menu'
}

export enum AuthenticatedRoutes {
  Home = '',
  Tasks = 'tasks',
  Task = 'task',
  Inventory = 'inventory',
  Account = 'account'
}

export enum AccountRoutes {

}
